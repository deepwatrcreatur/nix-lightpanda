name: Update Package

on:
  workflow_dispatch:
  schedule:
    - cron: "17 6 * * 1"

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@main

      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Update package
        run: nix shell nixpkgs#nix-update -c nix-update lightpanda --flake --version=stable --use-github-releases

      - name: Build package
        run: nix build .#lightpanda

      - name: Create pull request
        uses: peter-evans/create-pull-request@v7
        with:
          branch: automation/update-package
          commit-message: "chore: update lightpanda package"
          title: "chore: update lightpanda package"
          body: |
            Automated package update generated by `nix-update`.
